{
  "name": "ctc_srv",
  "version": "1.0.0",
  "scripts": {
    "start": "node srv/server",
    "build": "mbt build",
    "deploy": "mbt build && cf deploy mta_archives/CTC_srv_0.0.1.mtar",
    "install": "cd srv && npm install"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "make": "^0.8.1"
  }
}
